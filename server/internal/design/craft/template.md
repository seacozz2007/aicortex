# Template Craft

- Produce reusable structure with clear placeholder content blocks.
- Document sections with HTML comments (`<!-- section: hero -->`).
- Use CSS variables for all theme values so adopters can reskin easily.
- Avoid hard-coded product names unless specified in the brief.
- Tag editable regions with `data-aicortex-id`.
